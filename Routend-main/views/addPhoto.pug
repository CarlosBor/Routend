doctype html
html
  head
    title Add New Photo
  body
    include navbar.pug
    h1 Add New Photo
    form(action=`/photos/${tripId}` method="POST")
      label(for="url") Photo URL:
      input(type="text" name="url" required)
      br
      button(type="submit") Add Photo
